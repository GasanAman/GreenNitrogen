<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MyAlertDialogStyle" parent="android:Theme.DeviceDefault.Light.Dialog.Alert"> <!-- Used for the buttons -->

        <item name="colorAccent">@color/redNitrogen</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <!-- Used for the background -->
        <item name="android:windowBackground">@android:color/background_light</item>
        <item name="android:colorAccent">@color/redNitrogen</item>
    </style>
</resources>