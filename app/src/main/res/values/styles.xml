<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/redNitrogen</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/redNitrogen</item>
        <!--<item name="drawerArrowStyle">@style/DrawerArrowStyle</item>-->
        <!--<item name="colorControlNormal">@color/redNitrogen</item>-->
        <item name="colorControlActivated">@color/redNitrogen</item>
        <item name="alertDialogTheme">@style/AppTheme.Dialog.Alert.Red</item>
        <item name="dialogTheme">@style/AppTheme.Dialog.Red</item>
    </style>

    <style name="AppTheme.Dialog.Red" parent="Theme.AppCompat.Light.Dialog"> <!-- Used for the buttons -->
        <item name="colorPrimary">@color/redNitrogen</item>
        <item name="colorPrimaryDark">@color/redNitrogen</item>
        <item name="colorAccent">@color/redNitrogen</item>
        <!--<item name="colorAccent">@color/redNitrogen</item>-->
        <!-- Used for the title and text -->
        <!--<item name="android:textColorPrimary">@android:color/black</item>-->
        <!--<item name="android:windowBackground">@android:color/background_light</item>-->
        <!-- Used for the background -->
    </style>

    <style name="AppTheme.Dialog.Alert.Red" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/redNitrogen</item>
        <item name="colorPrimaryDark">@color/redNitrogen</item>
        <item name="colorAccent">@color/redNitrogen</item>
    </style>

    <style name="spinner_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">20dp</item>
        <item name="android:background">@drawable/edittext_style</item>
        <item name="android:popupBackground">#DFFFFFFF</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/indigo</item>
        <item name="colorButtonNormal">@color/pink</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
	
	
	
	<style name="TextViewAppearance.Headline2" parent="@style/TextViewAppearance">
        <item name="typeface">robotoLight</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/global_color_primary_light</item>
    </style>

    <style name="TextViewAppearance" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="typeface">robotoRegular</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:paddingLeft">26dp</item>
        <item name="android:paddingRight">26dp</item>
        <!-- Supports API Level 17+ -->
        <!-- <item name="android:paddingStart">26dp</item> -->
        <!-- Supports API Level 17+ -->
        <!-- <item name="android:paddingEnd">26dp</item> -->
    </style>

</resources>
